{
  "name": "local-html-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build-xlsx2md.mjs && npm test",
    "cli": "node scripts/xlsx2md-cli.mjs"
  },
  "devDependencies": {
    "@material/web": "^2.4.1",
    "esbuild": "^0.27.3",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
