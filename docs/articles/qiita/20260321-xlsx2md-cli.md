# `xlsx2md` CLI 追加記事メモ

## 記事テーマ

- `xlsx2md` に Node CLI を追加した話を書く
- 主役は Node CLI で何ができて、どう使うのかに置く
- 開発体験そのものは別記事に分ける

## この記事で伝えたいこと

- `xlsx2md` が画面操作だけでなく、バッチ的にも使えるようになった
- Node CLI から Markdown や ZIP を出力できる
- 少数のオプションで CLI を使える

## 想定読者

- GUI 操作なしで Excel ブックを Markdown に変換したい人
- `xlsx2md` を Node CLI として使いたい人
- `xlsx2md` の新しい使い方に興味がある人

## 見出し案

- はじめに
- Node CLI でできること
- まとめ

----------------------------------------------------------------

## 掲載先情報

- 掲載先: Qiita
- 公開記事タイトル: `xlsx2md` に Node CLI を追加しました
- URL: https://qiita.com/igapyon/items/869e25af98c3849c9ef8
- スクリーンショット挿入: 未確認

## Qiita 掲載用属性情報

- タイトル: `xlsx2md` に Node CLI を追加した話
- タグ: `Node.js`, `CLI`, `XLSX`, `Markdown`, `変換ツール`

----------------------------------------------------------------

## はじめに

先日、Excel ブックを生成 AI 向け Markdown に変換する `xlsx2md` という Single-file Web App を作りました。

もともとの `xlsx2md` は、`xlsx2md.html` をブラウザで開いて使う GUI 中心のアプリです。そこに今回、Node から実行できる CLI を追加しました。つまり、画面から使うだけでなく、バッチ的にも利用できるようにしようとしたものです。

この記事では、`xlsx2md` が CLI として何ができて、どう使うのかに絞って書きます。開発体験については別記事に分けます。

- 初出: 2026-03-21
- 更新: 2026-03-21

## Node CLI でできること

Node CLI を使うと、コマンドラインから次のように xlsx (Excelブック) から md (Markdown) への変換出力ができます。

- 単一の `.xlsx` workbook 入力
- 結合済み Markdown のファイル出力
- ZIP のファイル出力
- `--table-detection-mode border-priority` の利用
- `--shape-details include` の利用
- `--summary` によるシートごとの summary の標準出力
- `--help` による使い方の確認

<!-- IMAGE_ANCHOR: markdown-output-example -->

Excelブック・ファイルを入力に Markdown を出力するには以下のように指定します。

```bash
npm run cli -- ./tests/fixtures/xlsx2md-basic-sample01.xlsx --out /tmp/xlsx2md-basic.md
```

画像ファイルも含めて変換したい場合には ZIP ファイルの出力を利用します。

```bash
npm run cli -- ./tests/fixtures/xlsx2md-basic-sample01.xlsx --zip /tmp/xlsx2md-basic.zip
```

図形のテキスト出力が欲しい場合には、`--shape-details include` を付けて実行します。

```bash
npm run cli -- ./tests/fixtures/shape/shape-basic-sample01.xlsx --shape-details include
```

表の誤検知を抑えたい workbook では、`--table-detection-mode border-priority` を指定できます。

```bash
npm run cli -- ./tests/fixtures/table/table-border-priority-sample01.xlsx --table-detection-mode border-priority
```

変換が失敗した場合には、`read failed`、`parse failed`、`convert failed`、`zip write failed`、`markdown write failed` のように段階がわかるメッセージが出るようになっています。

## まとめ

`xlsx2md` はこれまでブラウザ画面から使う GUI 中心のアプリでしたが、今回の追加で Node CLI からも使えるようになりました。

その結果、Excel ブックをコマンドラインから Markdown や ZIP に変換できるようになっています。README や `--help`、エラーメッセージも整えてあるので、バッチ的に使いたい場合の入口として扱いやすくなったと思っています。

ぜひ活用して、Excel ブックを Markdown に変換する用途に役立ててもらえればと思います。

## 実行ページとソースコード

ソースコードは GitHub で公開しており、これをダウンロードして Node CLI を起動できます。

- https://github.com/igapyon/xlsx2md

従来のブラウザですぐ試せる Web 画面の実行ページは、次の URL です。

- https://igapyon.github.io/xlsx2md/xlsx2md.html

## 関連記事

- Excelブックを生成AI向けMarkdownに変換する `xlsx2md` を作りました
  - https://qiita.com/igapyon/items/cfbbc0d6112059b26522
- VS Code + GPT-5.4 と会話しながら、アプリ開発をかなり広い範囲で進めた話
  - https://qiita.com/igapyon/items/7a6ac820f8c3d2dc1069
